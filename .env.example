# Samba connection
SMB_HOST=192.168.1.100
SMB_SHARE=myshare
SMB_USER=myuser
SMB_PASSWORD=mypassword
SMB_DOMAIN=WORKGROUP

# Optional: subdirectory within the share (default: root)
SMB_PATH=/

# Sync interval in seconds (default: 300 = 5 minutes)
SYNC_INTERVAL=300
